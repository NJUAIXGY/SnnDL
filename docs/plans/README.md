# Active Plans

The implementation roadmap is
`2026-08-04-snndl-v5-cycle-model-roadmap.md`. It defines P0-P7 for the SST-native
cycle, storage, NoC, workload, and energy model. P0.0-P0.10 are accepted; the
sign-off records accept P0 and P1; P1 is compute-timed development evidence
only, while performance remains fail-closed until P4.

The v5 authority order and accepted boundary are recorded in
`../adr/README.md` and `../adr/0001-v5-contract-boundary.md`.

`2026-08-03-snndl-nextgen-2d-non-gas-architecture.md` remains the functional v4
semantic foundation and archive policy. Earlier PE, workload, memory front-end,
and experiment plans are retained under `archive/legacy_gas/docs/plans/` and
are not implementation instructions.
