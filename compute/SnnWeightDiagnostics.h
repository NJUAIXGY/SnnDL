#ifndef SST_ELEMENTS_SNNDL_COMPUTE_SNNWEIGHTDIAGNOSTICS_H
#define SST_ELEMENTS_SNNDL_COMPUTE_SNNWEIGHTDIAGNOSTICS_H

// 兼容 shim：历史路径 `compute/SnnWeightDiagnostics.h` 迁移到 `synapse/weights/SnnWeightDiagnostics.h`。
// 说明：避免重复定义与 include guard 冲突；实现以 synapse/weights 版本为单一真源。
#include "synapse/weights/SnnWeightDiagnostics.h"

#endif // SST_ELEMENTS_SNNDL_COMPUTE_SNNWEIGHTDIAGNOSTICS_H
